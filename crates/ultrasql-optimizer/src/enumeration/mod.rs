//! Join enumeration — populated in wave 6b.
//!
//! This module will contain the Cascades-style top-down search with
//! memoization, DPsize for ≤ 10 relations, and the greedy/GEQO heuristic
//! for larger queries. It is intentionally empty in wave 6a so that wave 6b
//! can populate it without touching `lib.rs`.

#[allow(dead_code)]
/// Placeholder enumeration context; wave 6b replaces this with real search.
struct Enumerator;
