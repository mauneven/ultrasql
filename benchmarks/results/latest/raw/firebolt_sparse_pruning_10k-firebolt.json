{
  "schema_version": 1,
  "engine": "firebolt",
  "workload": "firebolt_sparse_pruning_10k",
  "suite": "firebolt_sparse_pruning",
  "profile": "smoke",
  "n_rows": 10000,
  "warmup": 0,
  "iters": 1,
  "status": "not_available",
  "reason": "firebolt_url_missing",
  "generated_at_unix": 1779334902,
  "required_metrics": [
    "median_us",
    "min_us",
    "load_time_us",
    "primary_index_pruning_evidence"
  ],
  "required_shape": {
    "ddl": "CREATE FACT TABLE fact_events (...) PRIMARY INDEX event_day, tenant_id, bucket WITH (index_granularity = 1024)",
    "query": "SELECT event_day, tenant_id, bucket, SUM(amount), COUNT(*) FROM fact_events WHERE event_day BETWEEN ? AND ? AND tenant_id = 7 GROUP BY event_day, tenant_id, bucket"
  },
  "policy": "No Firebolt sparse-pruning benchmark claim exists until a configured FIREBOLT_URL emits measured samples and a raw EXPLAIN artifact shows primary-index pruning evidence."
}
